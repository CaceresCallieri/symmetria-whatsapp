# Note: the Qt/CMake ignore rules were removed with the Qt sources on this
# branch. `main` still holds that implementation, so keep them there and
# resolve in favour of this file's rules when the branches merge.

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project
*.AppImage

# Node / Electron
node_modules/
dist/
out/

# Built Surfingkeys bundle (6.6 MB of webpack output; rebuild per spike README)
spike/surfingkeys-electron/surfingkeys-ext/

# Vendored Surfingkeys build (produced by npm run build:extension)
vendor/
